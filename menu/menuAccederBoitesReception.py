from menu_general import MenuDisplay

if __name__ == "__main__":
    menu = ['Vider la messagerie', 'Selectionner un message', 'Quitter']
    # MenuDisplay(menu)
    # elmenu(menu)

    elmenu = MenuDisplay(menu)
