from menu_general import MenuDisplay

if __name__ == "__main__":
    menu = ['Supprimer un profil', 'Envoyer un message', 'Quitter']
    # MenuDisplay(menu)
    # elmenu(menu)

    elmenu = MenuDisplay(menu)
