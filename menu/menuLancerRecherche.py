from menu_general import MenuDisplay

if __name__ == "__main__":
    menu = ['Alerter un administrateur', 'Matcher', 'Passer au profil suivant', 'Quitter']
    # MenuDisplay(menu)
    # elmenu(menu)

    elmenu = MenuDisplay(menu)
