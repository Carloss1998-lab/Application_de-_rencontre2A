from menu_general import MenuDisplay

if __name__ == "__main__":
    menu = [' Selectionner les nouveaux centres d\'intérêts', 'Quitter']
    # MenuDisplay(menu)
    # elmenu(menu)

    elmenu = MenuDisplay(menu)
    input()
