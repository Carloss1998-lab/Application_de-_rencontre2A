from menu_general import MenuDisplay

if __name__ == "__main__":
    menu = ['Modifier le profil recherche', 'Modifier la portée', 'Quitter']
    # MenuDisplay(menu)
    # elmenu(menu)

    elmenu = MenuDisplay(menu)
