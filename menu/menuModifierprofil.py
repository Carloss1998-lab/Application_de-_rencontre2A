from menu_general import MenuDisplay

if __name__ == "__main__":
    menu = [' Modifier la description', 'Modifier les centres d\'intérêts', 'Modifier les préférences',
            'Ajouter une photo de profil', 'Quitter']
    # MenuDisplay(menu)
    # elmenu(menu)

    elmenu = MenuDisplay(menu)
