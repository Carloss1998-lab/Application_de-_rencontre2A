from menu.menu_general import MenuDisplay

if __name__ == "__main__":
    menu = ['Répondre au message', 'Supprimer le message', 'Afficher l\'historique des messages', 'Quitter']
    # MenuDisplay(menu)
    # elmenu(menu)

    elmenu = MenuDisplay(menu)
